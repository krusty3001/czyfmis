%table
  %tr
    %th
    %th Artist    
    %th Title
    %th Released at
    %th Tracks
    %th User
  - albums.each do |album|
    %tr
      %td= image_tag album.cover.url(:thumb) if album.cover.file?
      %td= album.artist.name      
      %td= link_to album.title, album
      %td= album.released_at
      %td= album.tracks.size
      %td= album.artist.user.email